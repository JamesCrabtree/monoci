apiVersion: skaffold/v1beta2
kind: Config
build:
  artifacts:
  - image: changeme
    context: .
    docker: {}
  tagPolicy:
    envTemplate:
      template: '{{.DOCKER_REGISTRY}}/jamescrabtree/monoci:{{.VERSION}}'
  local: {}
deploy:
  kubectl: {}
profiles:
- name: dev
  build:
    artifacts:
    - docker: {}
    tagPolicy:
      envTemplate:
        template: '{{.DOCKER_REGISTRY}}/jamescrabtree/monoci:{{.DIGEST_HEX}}'
    local: {}
  deploy:
    helm:
      releases:
      - name: monoci
        chartPath: charts/monoci
        setValueTemplates:
          image.repository: '{{.DOCKER_REGISTRY}}/jamescrabtree/monoci'
          image.tag: '{{.DIGEST_HEX}}'
